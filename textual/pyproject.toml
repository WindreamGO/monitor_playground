[project]
name = "trading-monitor-textual"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "textual>=8.0.0",
    "textual-serve>=1.1.0",
    "httpx>=0.27.0",
]

[dependency-groups]
dev = [
    "textual-dev",
]
